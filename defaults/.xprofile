#!/bin/bash
[[ -f ~/.config/zsh/.zshenv ]] && source ~/.config/zsh/.zshenv

# Load and execute custom profiles
if [ -d ~/.scripts ] ; then
    if [ -f ~/.scripts/xprofile ] ; then
        source ~/.scripts/xprofile
    fi
fi
